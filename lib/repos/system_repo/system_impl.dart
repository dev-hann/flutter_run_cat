import 'package:flutter_run_cat/models/system.dart';

import 'package:flutter_run_cat/controllers/controllers.dart';

import 'system_repo.dart';

class SystemImpl extends SystemRepo {
  @override
  Future init() async {}

  @override
  void addModelListener(ModelCallback<System> listener) {
  }

  @override
  void removeModelListener(ModelCallback<System> listener) {
  }
}
